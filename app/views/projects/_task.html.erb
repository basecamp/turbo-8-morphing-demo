<tr id="<%= dom_id task %>">
  <td>
    <%= form_with model: task do |form| %>
      <%= form.label :completed, class: "checkbox", for: dom_id(task, :completed) do %>
        <%= form.check_box :completed, class: "checkbox", id: dom_id(task, :completed) %>
      <% end %>
    <% end %>
  </td>
  <td>
    <%= task.title %>
  </td>
</tr>
